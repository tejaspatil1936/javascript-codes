const languages = {
    cpp: "c++",
    js: "javascript",
    py: "python"
}
//to print keys of an object
    for (const shortcut in languages) {
        console.log(shortcut);
    }
//to print values of key
    // for (const shortcut in languages) {
    //     console.log(languages[shortcut]);
    // }